PORT=3000
DATABASE_URL=postgresql://user:password@host:5432/agap?sslmode=require

# Must exactly match JWT_SECRET on the insighted-hq-v2 server. HQ uses this
# shared secret to sign the short-lived AGAP Applicants handoff token.
JWT_SECRET=replace-with-the-same-long-random-secret-used-by-insighted-hq-v2
# Only this verified InsightED HQ account may receive applicant access.
AGAP_SSO_EMAIL=wilfredo.cabral@deped.gov.ph
